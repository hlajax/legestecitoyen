	<%=render'menus/menu' %>
	<div class="container m-top-mini">
		<div class="row">
				<div class="col-lg-12 col-12"><h2 class="text-center text-maj"><%= @petition.titre %></h2></div>
				<div class="col-lg-8 col-12 m-top-mini">
						<%= image_tag @petition.image.url, class:"img-fluid"%>
						<div class="contenu"><%= raw(@petition.contenu) %></div>
						<div class="jumbotron">
  							<div class="row">
  								<div class="col-lg-8 col-6">
  										<h2 class="display-5">Lancez une pétition!</h2>
  							<p class="lead">
  											La personne qui a lancé cette pétition a décidé d'agir. Ferez-vous la même chose ?
							</p>
  							
  								</div>
  								
  								<div class="col-lg-4 col-6">
  							<a class="btn btn-primary" href="#" role="button">Lancez une pétition</a>
  								</div>
  								
  							</div>
						</div>
				</div>
				<div class="col-lg-4 col-12">
						<p>Pétition initiée par <%= @petition.citoyen.email%></p>
						<p><%= @petition.petition_cat.titre%></p>
						<p><%=@petition.votes_for.size%> signature(s) pour cette pétition</p>
						<%= link_to "Signer la pétition", like_petition_path(@petition),class:"btn btn-geste", method: :put %>
				</div>
		</div>
</div>
<%= link_to 'Edit', edit_petition_path(@petition) %> |
<%= link_to 'Back', petitions_path %>
