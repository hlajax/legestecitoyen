<%= form_with(model: petition_cat, local: true) do |form| %>
  <% if petition_cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(petition_cat.errors.count, "error") %> prohibited this petition_cat from being saved:</h2>

      <ul>
        <% petition_cat.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titre %>
    <%= form.text_field :titre %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
